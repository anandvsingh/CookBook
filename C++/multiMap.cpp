#include <iostream>
#include <map>

using namespace std;

int main()
{
    multimap <int,string> mp;
    // same as map except it allows duplicate keys
    // Also it only use insert(make_pair()) to insert values
}